# Installer

The production installer definition will live here. It must support per-user or
per-machine installation as selected later, preserve `Data` during upgrades, and
register a clean uninstall path.

